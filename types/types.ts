export type ContactUsType = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  message: string;
};

export type PartnerRequestType = {
  companyName: string;
  contactPerson: string;
  contactEmail: string;
  message: string;
};
